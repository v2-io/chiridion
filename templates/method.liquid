{% comment %}
Method documentation template.
Variables:
  - display_name: Method name with class prefix if needed
  - has_params: Boolean - whether method has parameters
  - docstring: Method description (already linkified, may be nil)
  - params: Array of formatted param strings (e.g., "⟨name : Type⟩ → desc")
  - return_line: Formatted return string (e.g., "→ ReturnType — desc")
  - examples: Array of {name:, text:}
  - behaviors: Array of behavior strings (from specs)
  - spec_examples: Array of {name:, code:}
{% endcomment %}
### {{ display_name }}{% if has_params %}(...){% endif %}
{% if docstring %}

{{ docstring }}{% endif %}
{% if params.size > 0 or return_line %}

{% for param in params %}{{ param }}
{% endfor %}{% if return_line %}{{ return_line }}{% endif %}{% endif %}
{% for ex in examples %}

**{% if ex.name %}Example: {{ ex.name }}{% else %}Example{% endif %}:**

```ruby
{{ ex.text }}
```{% endfor %}
{% if behaviors.size > 0 %}

**Tested behaviors:**
{% for b in behaviors %}- {{ b }}
{% endfor %}{% endif %}
{% for ex in spec_examples %}

**From specs ({{ ex.name }}):**

```ruby
{{ ex.code }}
```{% endfor %}
